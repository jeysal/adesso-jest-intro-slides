import { Invert } from 'mdx-deck';

import jestCoverageRun from '!!file-loader!../media/jest-coverage-run.webm';

<Invert>

<video
  controls
  style={{ maxWidth: '100%', maxHeight: '100%', objectFit: 'scale-down' }}
>
  <source src={jestCoverageRun} type="video/webm" />
</video>

</Invert>

<Notes>

BUT if we run Jest with --coverage the missing test immediately comes back to us

nice table in the console showing coverage stats per tested file

BTW the todo test also appears in the summary

</Notes>
