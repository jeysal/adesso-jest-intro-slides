import { Slide } from '../adesso';

<Slide>

# TypeScript

<div style={{margin:'1rem'}}>
<span style={{fontFamily: 'monospace'}}>package.json</span>

```diff
 {
   "license": "MIT",
   "devDependencies": {
     "@babel/core": "^7.6.0",
     "@babel/preset-env": "^7.6.0",
+    "@babel/preset-typescript": "^7.6.0",
     "jest": "^24.9.0"
   },
   "babel": {
     "presets": [
-      "@babel/preset-env"
+      "@babel/preset-env",
+      "@babel/preset-typescript"
     ]
   }
 }
```

</div>

</Slide>

<Notes>

also, Jest supports TypeScript out of the box

we can just add the TypeScript preset to our compiler configuration

and because Jest calls into babel to transform the source files, it'll pick that up automatically

neither for coverage nor for typescript did we need any config

</Notes>
