import { Invert } from 'mdx-deck';

import jestParallelization from '!!file-loader!../media/jest-parallelization.webm';

<Invert>

<video
  autoplay
  loop
  style={{ maxWidth: '100%', maxHeight: '100%', objectFit: 'scale-down' }}
>
  <source src={jestParallelization} type="video/webm" />
</video>

</Invert>

<Notes>

need to take care of parallelization if you want to deploy Jest at scale

Once you hit a certain threshold, Jest will automatically start running your tests in parallel

This is done by spawning worker processes depending on the #CPUs

And also Jest is really smart about parallelization, remembers how long your tests each take and runs the slowest ones first -> explain optimization

</Notes>
